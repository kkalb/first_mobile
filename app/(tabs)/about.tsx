import { Text, View } from 'react-native';

const AboutScreen = () => {
  return (
    <View className="flex">
      <Text className="flex">About screen</Text>
    </View>
  );
};

export default AboutScreen;
